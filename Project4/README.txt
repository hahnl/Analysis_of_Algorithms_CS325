Using the command line, compile via:
	g++ -o project4 main.cpp

To run the program:
	./project4 [filename.txt]

The program will open the file [filename.txt], then will 
output the results in [filename.txt.tour].